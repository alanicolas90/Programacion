package cachos;

import javax.print.DocFlavor;

public class
cachitos {
    StringBuilder nombre = new StringBuilder();

/*
    string.replace(string que quieres cambiar, el valor nuevo);
    string.append();
    string.subtring(inicial,final);
    string.length();
    string.indexOf("string", posicion de inicio); te devuelve desde donde empieza el string entre comillas
    string.lastIndexOf("string", posicion del final);
    string.contains();
   .split();

   CIFRADOS____________________________
    car=(char)(car-65)
   car = (char)(car+3%26; %26)+65 (modulo 26 cuando supere el 26 vuelve a 1). SOLO PAAR SUMAS
   car=(char)(((car-3+26)%26)+65) (para que siempre sea positivo)






    ctr*enter --- ctrl+q
*/


}
